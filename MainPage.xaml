<Page
    x:Class="FiaMedKnuffGrupp4.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FiaMedKnuffGrupp4"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:Microsoft.Toolkit.Uwp.UI"
    
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="Orange" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
       
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
       
        <Button x:Name="StartButton" Width="300" Height="100" Click="Button_Start" HorizontalAlignment="Center" Margin="0,190,0,0" Grid.Row="5" VerticalAlignment="Top">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="10" Background="#FF8832">
                        <Grid>
                            <TextBlock Text="Start" FontSize="70" Foreground="#FFFFFF" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button x:Name="CreateButton" Width="300" Height="100" Click="Button_Create" HorizontalAlignment="Center" Margin="0,318,0,0" Grid.Row="5" VerticalAlignment="Top">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="10" Background="#FF8832">
                        <Grid>
                            <TextBlock Text="Create player" FontSize="45" Foreground="#FFFFFF" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button x:Name="LoadButton" Width="300" Height="100" Click="Button_Load" HorizontalAlignment="Center" Margin="0,444,0,0" Grid.Row="5" VerticalAlignment="Top" >
            <Button.Template>
                <ControlTemplate TargetType="Button" >
                    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="10"  Background="#FF8832">
                        <Grid>
                            <TextBlock Text="Load player" FontSize="50" Foreground="#FFFFFF" FontWeight="SemiBold"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button x:Name="SaveButton" Width="300" Height="100" Click="Button_Save" HorizontalAlignment="Center" Margin="0,570,0,0" Grid.Row="5" VerticalAlignment="Top" >
            <Button.Template>
                <ControlTemplate TargetType="Button" >
                    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="10"  Background="#FF8832">
                        <Grid>
                            <TextBlock Text="Save player" FontSize="50" Foreground="#FFFFFF" FontWeight="SemiBold"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button x:Name="ScoreButton" Width="300" Height="100"  Click="Button_Score" HorizontalAlignment="Center" Margin="0,694,0,0" Grid.Row="5" VerticalAlignment="Top" >
            <Button.Template>
                <ControlTemplate TargetType="Button" >
                    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="10"  Background="#FF8832">
                        <Grid>
                            <TextBlock Text="High score" FontSize="50" Foreground="#FFFFFF" FontWeight="SemiBold"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button x:Name="QuitButton" Width="300" Height="100" Click="Button_Quit" HorizontalAlignment="Center" Margin="0,818,0,0" Grid.Row="5" VerticalAlignment="Top" >
            <Button.Template>
                <ControlTemplate TargetType="Button" >
                    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="10"  Background="#FF8832">
                        <Grid>
                            <TextBlock Text="Quit" FontSize="70" Foreground="#FFFFFF" FontWeight="SemiBold"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Page>
